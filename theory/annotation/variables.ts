// Object annotation
const coordinate: { x: number; y: number } = {
  x: 10,
  y: 20,
};

// Function annotation
const helloPerson: (name: string) => void = (name) => {
  console.log(`Hello, ${name}`);
};
